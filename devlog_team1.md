개별 개발 일지

프로젝트명: 전자 기기 전력 소비 모니터링 및 절전 분석 시스템
팀명: 파워 레코드
이름: 이민혁
학번: 202633326
담당 역할: 기기 정보 입력 기능 구현 및 1D/2D 배열을 활용한 전력 데이터 저장 기능 구현

1. 담당 기능 개요

이번 팀 프로젝트에서 나는 사용자가 전자기기의 정보를 입력하고, 입력된 데이터를 배열에 저장하는 기능을 담당하였다. 구체적으로 기기명, 위치, 정격전력, 전압, 전류, 하루 사용 시간을 입력받는 기능을 구현하였고, 입력된 정보를 구조체 배열에 저장하도록 하였다. 또한 기기별 시간대별 전력 사용량을 저장하기 위해 2차원 배열 powerLog[MAX_DEVICES][HOURS]를 사용하였다. 이를 통해 여러 전자기기의 전력 데이터를 체계적으로 관리할 수 있도록 하였다.

2. 개발 내용

처음에는 전자기기 정보를 각각의 변수로 따로 저장하려고 했지만, 기기 수가 늘어나면 관리가 복잡해진다는 문제가 있었다. 그래서 팀 회의 후 Device devices[MAX_DEVICES] 구조체 배열을 사용하여 최대 8개의 기기 정보를 저장하는 방식으로 수정하였다. 사용자가 입력한 기기명, 위치, 정격전력, 전압, 전류, 사용 시간은 inputDevice 함수에서 입력받도록 하였다.
입력 기능에서는 잘못된 값이 들어가지 않도록 최소값을 검사하는 함수를 사용하였다. 예를 들어 정격전력은 0보다 큰 값이 입력되어야 하고, 사용 시간은 하루 기준 24시간을 넘지 않도록 제한하였다. 이를 통해 프로그램 실행 중 비정상적인 계산이 발생하는 것을 줄일 수 있었다.
또한 기기별 시간대별 전력 사용량을 저장하기 위해 2차원 배열을 활용하였다. powerLog[MAX_DEVICES][HOURS] 배열에서 첫 번째 인덱스는 기기 번호를 의미하고, 두 번째 인덱스는 0시부터 23시까지의 시간을 의미하도록 설계하였다. makePowerLog 함수에서는 사용 시간이 남아 있는 동안 해당 시간대에 소비전력을 저장하고, 사용 시간이 끝나면 나머지 시간대에는 0을 저장하도록 구현하였다.

3. 주요 구현 코드 설명

내가 담당한 핵심 함수는 inputDevice 함수와 makePowerLog 함수이다. inputDevice 함수는 사용자가 입력한 기기 정보를 구조체에 저장하는 역할을 한다. 이 함수에서는 기기명, 위치, 정격전력, 전압, 전류, 사용 시간을 입력받고, 전압과 전류를 이용해 소비전력을 계산할 수 있도록 필요한 데이터를 저장한다.
makePowerLog 함수는 기기별 하루 전력 사용 기록을 2차원 배열에 저장하는 역할을 한다. 예를 들어 어떤 기기의 하루 사용 시간이 6시간이라면, 24시간 중 앞의 6시간에는 해당 기기의 소비전력을 저장하고 나머지 시간에는 0을 저장한다. 이 배열은 이후 평균 소비전력과 최대 소비전력을 계산하는 데 사용된다.

4. 어려웠던 점과 해결 과정

개발 과정에서 가장 어려웠던 점은 2차원 배열의 의미를 정확히 이해하고 적용하는 것이었다. 처음에는 powerLog[i][h]에서 i와 h가 각각 무엇을 의미하는지 헷갈렸지만, i는 기기 번호, h는 시간대라고 정리하면서 이해할 수 있었다. 이후 반복문을 사용해 기기별, 시간대별 데이터를 저장하도록 구현하였다.
또한 사용자가 잘못된 값을 입력했을 때 프로그램이 이상하게 작동할 수 있다는 점도 고려해야 했다. 예를 들어 사용 시간이 24시간을 초과하거나, 정격전력이 0으로 입력되면 전력 분석 결과가 부정확해질 수 있다. 이를 해결하기 위해 입력값을 검사하는 함수를 사용하였고, 사용 시간이 24시간을 넘으면 24시간으로 조정되도록 처리하였다.

5. 개발 후 느낀 점

이번 프로젝트를 통해 배열과 구조체를 함께 사용하면 여러 데이터를 훨씬 체계적으로 관리할 수 있다는 것을 알게 되었다. 특히 2차원 배열은 처음에는 어렵게 느껴졌지만, 기기별 시간대별 전력 데이터를 저장하는 데 매우 적합하다는 것을 이해하게 되었다. 또한 단순히 값을 입력받는 기능이라도 입력값 검증을 제대로 하지 않으면 프로그램 전체 결과에 영향을 줄 수 있다는 점을 배웠다.
내가 구현한 입력 및 배열 저장 기능은 이후 전력 계산, 대시보드 출력, 파일 저장 기능의 기초 데이터가 되기 때문에 프로젝트에서 중요한 부분이라고 생각한다. 이번 경험을 통해 C언어에서 배열, 구조체, 반복문을 실제 프로그램에 적용하는 방법을 더 잘 이해할 수 있었다.
